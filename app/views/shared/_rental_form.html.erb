
<%= simple_form_for([@parking, @rental]) do |f| %>
  <%= f.input :start_date, input_html: { class: 'dropdown' } %>
  <%= f.input :end_date, input_html: { class: 'dropdown' } %>
  <%= f.submit '¡Elige tus fechas!', class: 'btn button blue-button', id: 'paybutton' %>
<% end %>
