
<%= simple_form_for([@parking, @rental]) do |f| %>
  <%= f.date_field :start_date, min_max: true, min: Date.today, input_html: {  } %>
  <%= f.date_field :end_date, min_max: true, input_html: {  } %>
  <%= f.submit '¡Elige tus fechas!', class: 'btn button blue-button', id: 'paybutton' %>
<% end %>
