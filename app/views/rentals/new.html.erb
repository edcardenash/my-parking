<div class="text-center">
  <%= simple_form_for([@parking, @rental]) do |f| %>
    <%= f.input :start_date, html5: true %>
    <%= f.input :end_date html5: true %>
    <div class="form-actions">
      <%= f.button :submit, "Reserva un parking", class: "text-white" %>
    </div>
  <% end %>
</div>
